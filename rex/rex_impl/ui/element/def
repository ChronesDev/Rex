#pragma once

namespace rex::ui
{
    struct IElement;

    struct Element;
    struct ElementBase;
    struct ElementContentBase;
    struct ElementLayoutBase;

    struct ElementScope;

    struct ElementPath;
}